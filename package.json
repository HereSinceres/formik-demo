{
  "homepage": "http://HereSinceres.github.io/formik-demo",
  "name": "formik-demo",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "formik": "^1.2.0",
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-scripts": "1.1.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "push":"git add . && git commit -m 'auto' && git push origin master"
  },
  "git address": "https://github.com/HereSinceres/formik-demo.git",
  "devDependencies": {
    "gh-pages": "^1.2.0"
  }
}
